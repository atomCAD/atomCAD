// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.10.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `capture_screenshot_impl`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `fmt`

/// Capture the current viewport to a PNG file.
///
/// # Arguments
/// * `output_path` - Path where the PNG file will be written
/// * `width` - Optional width override (uses current viewport if None)
/// * `height` - Optional height override (uses current viewport if None)
/// * `background_rgb` - Background color as [R, G, B] (0-255), defaults to dark gray [30, 30, 30]
///
/// # Returns
/// `ScreenshotResult` indicating success/failure and metadata
ScreenshotResult captureScreenshot(
        {required String outputPath,
        int? width,
        int? height,
        Uint8List? backgroundRgb}) =>
    RustLib.instance.api.crateApiScreenshotApiCaptureScreenshot(
        outputPath: outputPath,
        width: width,
        height: height,
        backgroundRgb: backgroundRgb);

/// Result of a screenshot capture operation
class ScreenshotResult {
  final bool success;
  final String outputPath;
  final int width;
  final int height;
  final String? errorMessage;

  const ScreenshotResult({
    required this.success,
    required this.outputPath,
    required this.width,
    required this.height,
    this.errorMessage,
  });

  @override
  int get hashCode =>
      success.hashCode ^
      outputPath.hashCode ^
      width.hashCode ^
      height.hashCode ^
      errorMessage.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ScreenshotResult &&
          runtimeType == other.runtimeType &&
          success == other.success &&
          outputPath == other.outputPath &&
          width == other.width &&
          height == other.height &&
          errorMessage == other.errorMessage;
}
